#NumTrans.py
template = "零一二三四五六七八九"               #把零到九作为一个字符串，每一个字符在字符串的位置就是这个字符对应的数字
s = input()                                   #input默认输入为字符型，输入123，其实就是字符“123”，需要eval()函数变成数字123
for c in s:                                   #用For循环语句遍历字符串s，将字符串s的每一个字符分别赋值给c，循环一次，赋值一次
    print(template[eval(c)], end="")          #print()中增加end=""参数表示输出后不增加换行，多个print()可以连续输出
